#include<stdio.h>
int main(){
    float length,area;
	printf("Enter length of Triangle:");
	scanf("%f",&length);
	area=0.433*length*length; 
    printf("The Area of Triangle is : %f",area);
    return 0;  
}